name: wealthfolio
services:
  wealthfolio:
    image: docker.io/afadil/wealthfolio:3.2.1
    container_name: wealthfolio
    restart: unless-stopped
    volumes:
      - ./data:/data:Z
    env_file:
      - .env
    ports:
      - 7490:8088
